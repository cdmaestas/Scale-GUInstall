# Add IBM Storage Scale GPFS binaries to PATH.
# Installed by scale-guinstall so that mm* commands (mmlscluster, mmchconfig, …)
# are available in interactive shells without a full /usr/lpp/mmfs/bin prefix.
export PATH=$PATH:/usr/lpp/mmfs/bin
